services:
  db:
    image: mysql:8.4
    container_name: helpin-db
    environment:
      MYSQL_DATABASE: helpin
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - helpin-network

  # GRUP APACHE 
  apache:
    build:
      context: .
      dockerfile: ./docker-apache/Dockerfile
    container_name: helpin-apache
    profiles: ["apache"] # 
    ports:
      - "8000:80"
    volumes:
      - .:/var/www/html
    networks:
      - helpin-network

  # GRUP NGINX (Terdiri dari Nginx + PHP-FPM)
  php-fpm:
    build:
      context: .
      dockerfile: ./docker-nginx/Dockerfile
    container_name: helpin-php-fpm
    profiles: ["nginx"] # <--- Ikut grup nginx
    volumes:
      - .:/var/www/html
    networks:
      - helpin-network

  nginx:
    image: nginx:alpine
    container_name: helpin-nginx
    profiles: ["nginx"] # <--- Ikut grup nginx
    ports:
      - "8000:80"
      - "5173:5173" # misal pakai vite
    volumes:
      - .:/var/www/html
      - ./docker-nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - php-fpm
    networks:
      - helpin-network

networks:
  helpin-network:
    driver: bridge

volumes:
  mysql_data: